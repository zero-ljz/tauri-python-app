# Optional: expose the IPC debug panel in a production frontend build.
VITE_ENABLE_RPC_DEBUG=0

# Read at Rust compile time. Leave unset for development builds without updates.
TAURI_UPDATER_PUBLIC_KEY=
TAURI_UPDATER_ENDPOINT=https://github.com/OWNER/REPOSITORY/releases/latest/download/latest.json
